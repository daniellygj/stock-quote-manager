# Atock quote manager

### API documentation:
After running the project, access the url http://localhost:8080/swagger-ui.html


### How to run the project:
  * Run the commands
    ```
      mvn install
    
      mvn spring-boot:run
    ```
